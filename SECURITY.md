# 보안 운영 안내

## 비밀정보

- API 토큰과 키는 Git 저장소, README, 프런트엔드 번들, 이슈, 로그에 기록하지 않습니다.
- GitLab CI/CD 변수 또는 Cloudflare Secrets를 사용합니다.
- 토큰이 채팅·터미널·로그·커밋에 노출되었다고 의심되면 즉시 폐기하고 새 토큰으로 교체합니다.
- 서비스별 토큰은 필요한 범위의 최소 권한과 만료기간을 적용합니다.

## 내부 배포 권한

GitLab 내부 프로젝트는 기관의 인증된 사용자만 조회할 수 있도록 `Internal` 가시성을 사용합니다. 내부 프로젝트라고 해서 모든 기관 구성원에게 업무상 필요한 원본 데이터나 운영 비밀정보가 공개되는 것은 아니므로, 그룹·프로젝트·보호 브랜치 권한을 별도로 점검합니다.

## 애플리케이션 경계

- 브라우저에는 AI API 키를 전달하지 않습니다.
- Cloudflare Worker의 CORS 허용 출처와 요청 길이 제한을 유지합니다.
- Worker에서 외부 API 오류와 AI 오류를 사용자에게 필요한 수준으로만 반환합니다.
- 데이터 원문 링크를 제공하되, 원문 데이터의 이용조건과 최신성은 사용자가 확인해야 합니다.

## 점검 기준

배포 전 다음 항목을 확인합니다.

1. 의존성 취약점 검사
2. 정적 코드 보안 검사
3. 개인정보·비밀정보 데이터 흐름 검사
4. 프로덕션 빌드
5. 저장소에 비밀정보와 생성물·의존성 디렉터리가 포함되지 않았는지 확인

현재 점검에서 일부 의존성 업데이트 권고와 모바일 정적 서버의 동적 파일 제공 관련 경고가 확인되었습니다. 모바일 서버는 경로를 정적 루트 내부로 제한하고 허용된 플랫폼 경로만 사용하도록 보완했지만, 정적 분석 도구는 동적 파일 제공 자체를 보수적으로 경고합니다. 이는 내부용 웹 정적 배포와 별도 범위이지만, 모바일 서버를 외부에 노출하기 전 경로 이탈 시도에 대한 수동 테스트와 의존성 업데이트를 완료해야 합니다.

## 신고

취약점 신고에는 토큰, 개인정보, 운영 URL의 민감한 세부정보를 포함하지 않습니다. 저장소 관리자에게 비공개 채널로 재현 단계와 영향 범위만 전달합니다.